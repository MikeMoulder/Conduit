{
  "name": "stockpilot",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "description": "On chain investment mandate enforcement for tokenized equity portfolios",
  "scripts": {
    "build:ts": "tsc -p tsconfig.json",
    "test": "npm run build:ts && mocha",
    "assets:devnet": "npm run build:ts && node .test-build/scripts/create-devnet-assets.js"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.19.0",
    "chai": "^4.5.0",
    "mocha": "^10.8.2",
    "ts-mocha": "^10.1.0",
    "typescript": "^5.9.0"
  }
}
