# Conduit app

The Next.js site, the chat agent and the background worker for Conduit.

The project README, with the architecture, sponsor integrations, proof links
and setup, is at the repository root: [../README.md](../README.md).

```bash
cp .env.example .env
npm install
npm run dev     # http://localhost:3000
npm test        # app tests
```
